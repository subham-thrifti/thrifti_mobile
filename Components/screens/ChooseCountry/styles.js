import { StyleSheet } from 'react-native';
import colors from '../../../css/abstracts/variables'

const searchBarStyles = StyleSheet.create({
    inputContainerStyle: {
        backgroundColor: colors.lightgrey
    }
})

export { searchBarStyles, colors }